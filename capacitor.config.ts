import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'REGISTRAPP2-MAIN',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
