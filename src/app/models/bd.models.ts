export interface Usuario {  // Interface para los usuarios
      nombreCompleto: string;
      usuario: string;
      clave: string;
      telefono: string;
      email: string;
      rol:string;
      id: string;
}
